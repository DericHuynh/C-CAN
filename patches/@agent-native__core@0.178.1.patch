diff --git a/dist/agent/model-config.js b/dist/agent/model-config.js
index e888f8ca3a037057d98be5e2d68b11a74f537e08..8cfda94428d278654b4c3daa84e803b0dfeb4df1 100644
--- a/dist/agent/model-config.js
+++ b/dist/agent/model-config.js
@@ -18,7 +18,8 @@
 //  claude-*        → 200_000  (Haiku 4.5 and earlier models)
 //  gpt-5*          → 1_050_000 (GPT-5.6 Sol/Terra flagship context)
 //  gemini-2* / gemini-3* → 1_048_576
-//  everything else → 128_000  (safe conservative floor)
+//  deepseek-*          → 1_000_000 (app-catalog extension)
+//  everything else     → 128_000  (safe conservative floor)
 //
 // Note: Fable 5, Sonnet 5, Sonnet 4.6, and Opus 4.6/4.7/4.8 support 1M via the API
 // at standard prices.
@@ -60,6 +61,9 @@ const MODEL_CONTEXT_WINDOWS = {
     "gemini-3.1-pro-preview": 1_048_576,
     "gemini-2.5-flash": 1_048_576,
     "gemini-2.5-pro": 1_048_576,
+    // ── DeepSeek (custom engine registered by ICCPlus CYOA Studio) ──────────
+    "deepseek-v4-flash": 1_000_000,
+    "deepseek-reasoner": 1_000_000,
 };
 /** Conservative safe default when a model is not in the table. */
 const DEFAULT_CONTEXT_WINDOW = 128_000;
@@ -101,6 +105,9 @@ export function getContextWindowForModel(modelId) {
     // Z.ai GLM 5.x on OpenRouter — 1M context.
     if (id.startsWith("glm-5") || id.includes("/glm-5"))
         return 1_048_576;
+    // DeepSeek v4 (chat + reasoner) — 1M context (app-catalog extension).
+    if (id.startsWith("deepseek-"))
+        return 1_000_000;
     return DEFAULT_CONTEXT_WINDOW;
 }
 // ---------------------------------------------------------------------------
diff --git a/dist/collab/client.js b/dist/collab/client.js
index 692501c5212a358a6d608be4ada351c2996110ef..13676f56cb7c550befe859e1fb2481213bf0338c 100644
--- a/dist/collab/client.js
+++ b/dist/collab/client.js
@@ -197,6 +197,7 @@ class CollabDocConnection {
     lastSetUser = null;
     // Local-update batching (debounced + coalesced with Y.mergeUpdates).
     pendingUpdates = [];
+    updateRetryCount = 0;
     flushTimer = null;
     updateHandlerAttached = false;
     // Poll loop + SSE fast path.
@@ -631,7 +632,19 @@ class CollabDocConnection {
                 requestSource: this.requestSource,
             }),
             ...(keepalive ? { keepalive: true } : {}),
-        }).catch(() => { });
+        }).then((response) => {
+            if (!response.ok) throw new Error(`Live update failed (${response.status})`);
+            this.updateRetryCount = 0;
+            window.dispatchEvent(new CustomEvent("agent-native:collab-update-status", { detail: { docId: this.docId, error: null } }));
+        }).catch((error) => {
+            // Yjs updates are idempotent. Retain the exact failed bytes and
+            // coalesce them with subsequent typing instead of losing the batch.
+            this.pendingUpdates.unshift(merged);
+            window.dispatchEvent(new CustomEvent("agent-native:collab-update-status", { detail: { docId: this.docId, error: error instanceof Error ? error.message : "Live update failed" } }));
+            if (this.disposed || this.flushTimer) return;
+            const delay = Math.min(15000, 500 * 2 ** Math.min(this.updateRetryCount++, 5));
+            this.flushTimer = setTimeout(() => this.flushPendingUpdates(), delay);
+        });
     }
     // -------------------------------------------------------------------------
     // Remote sync: SSE fast path + poll fallback loop
diff --git a/src/eject/catalog-wrappers.spec.ts b/src/eject/catalog-wrappers.spec.ts
deleted file mode 100644
index 1ec0b6552639190d5bd9cfc993a5bf095554b445..0000000000000000000000000000000000000000
diff --git a/src/integrations/eject/slack.spec.ts b/src/integrations/eject/slack.spec.ts
deleted file mode 100644
index 2581340d41a2f571f62ebb069f36168e590681cd..0000000000000000000000000000000000000000
