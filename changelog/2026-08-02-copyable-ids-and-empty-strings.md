---
type: fixed
date: 2026-08-02
---

Editor: entity ids are now one-click copyable everywhere (except the viewer) via a small copy chip — the ID list is now a collapsible tree (rows expand to their choices, with a per-row choice count) whose ids copy on click, and image, design-group, sound-effect, variable and word ids are copy chips too. Empty strings are genuinely empty: the "Untitled row / choice / group / …" fallbacks and the "No image" select placeholders are gone, so a choice or row with no title shows nothing instead of invented text.
