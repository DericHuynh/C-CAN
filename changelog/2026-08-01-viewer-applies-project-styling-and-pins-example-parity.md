---
type: fixed
date: 2026-08-01
---

ICCPlus viewer now applies the document's styling to choice cards, row cards, text and row buttons (backgrounds, borders, radius, drop shadows, fonts/colors, padding) instead of dropping it; imported styling is pinned by a generated parity suite (`pnpm script generate-example-tests`)
